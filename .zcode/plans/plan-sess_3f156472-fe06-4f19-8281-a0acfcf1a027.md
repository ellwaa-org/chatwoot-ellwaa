Write the reviewed compose to `docker-compose.coolify.yaml` at the repo root exactly as provided by the user, with the single required fix applied:

- `rustfs` healthcheck: change `http://127.0.0.1:9000/health/ready` to `http://127.0.0.1:9000/health` (the endpoint RustFS's own compose and docs use — the `/health/ready` path would 404, fail the healthcheck, and stall create_bucket → rails → sidekiq on first deploy).

Everything else stays exactly as the user wrote it (services, env wiring, comments, header instructions). No other file changes; no application code changes.